name: CI/CD SGE Test with Newman

on:
  push:
    branches: [main, dev]
  pull_request: 
    branches: [main, dev]
    
    
jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      # 1️⃣ Récupération du code
      - name: Checkout repo
        uses: actions/checkout@v4
      
      # 2️⃣ Installation de Docker Compose (inclus dans docker-cli maintenant)  
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      # 3️⃣ Lancement du docker-compose (API + Postgres)  
      - name: Start Docker Compose
        run: docker compose -f compose.yaml up -d

      - name: Show compose services
        run: docker compose -f compose.yaml ps 
        
      - name: API logs
        run: docker compose -f compose.yaml logs sge_api
        
      - name: Show API logs
        run: docker compose -f compose.yaml logs sge_api
          
      # 4️⃣ Attendre que l’API soit prête
      #- name: Wait for API to be ready
      #  run:  |
      #    echo "Waiting for API (max 60s)..."
      #    for i in {1..30}; do
      #      status=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/departments)
      #      if [ "$status" -eq 200 ]; then
      #        echo "API is READY with status 200!"
      #        exit 0
      #      fi
      #      echo "API not ready yet (code: $status). Retrying..."
      #      sleep 2
      #    done
      #    echo "API did not start in time."
      #    docker compose -f compose.yaml logs
      #    exit 1
          
      # 5️⃣ Installation de Newman
      - name: Install Newman
        run: npm install -g newman
          
      # 🔵 Exécuter la collection Postman
      - name: Run Postman tests
        run: |
          newman run ./testPostman/SSGE_NET9_GITHUB_ACTION.postman_collection.json \
            --env-var base_url=http://host.docker.internal:8080 \
            --reporters cli,junit \
            --reporter-junit-export newman-results.xml
          
      # 🟡 Sauvegarder le rapport comme artefact
      - name: Upload Newman test report
        uses: actions/upload-artifact@v4
        with:
          name: newman-report
          path: newman-results.xml
            
      # 6️⃣ Affichage des logs en cas d’échec
      - name: Docker logs
        if: failure()
        run: docker compose -f compose.yaml logs